[package]
name = "aube_nif"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
aube = { path = "../../crates/aube", default-features = false }
rustler = "0.37.3"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time"] }
